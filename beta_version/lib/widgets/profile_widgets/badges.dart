import 'package:custom_ui/custom_ui.dart';

// FIXME
class BadgesList extends StatelessWidget {
  const BadgesList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('badges here'),
    );
  }
}
